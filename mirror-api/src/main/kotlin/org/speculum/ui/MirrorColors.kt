package org.speculum.ui

import androidx.compose.ui.graphics.Color

/**
 * MagicMirror's three brightness levels on a black background:
 * bright text, normal (dimmed) labels, and faint secondary text.
 */
object MirrorColors {
    val Bright = Color(0xFFFFFFFF)
    val Normal = Color(0xFF999999)
    val Dimmed = Color(0xFF666666)
}